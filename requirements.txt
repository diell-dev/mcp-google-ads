# MCP requirements
# Pinned to the last 1.x release. mcp 2.0.0 restructured the package and removed
# `mcp.server.fastmcp`, which this server imports. Do not bump to 2.x without
# migrating the FastMCP usage in google_ads_server.py.
mcp==1.29.0

# Google API requirements
google-auth>=2.25.2
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
requests>=2.31.0

# Environment configuration
python-dotenv>=1.0.0

# Optional visualization dependencies
matplotlib>=3.7.3
pandas>=2.1.4